# app_glm 完成情况汇报

> 日期：2026-07-05
> 覆盖范围：`app_glm/` 全部（Android + iOS + Shared + Web）
> 依据：代码实际文件扫描 + `docs/TODO-glm.md` + git log

---

## 一、总体完成度

| 平台 | 完成度 | 文件数 | 关键状态 |
|------|--------|--------|----------|
| **Android** | ~75% | 118 `.kt` 文件 | 20 Screen 齐全，RealCarRepository 已接入，Dashboard 已接真实数据 |
| **iOS** | ~65% | 39 `.swift` 文件 | 17 Feature 目录，5 页面缺失，图表仍用假数据 |
| **Shared** | ✅ 100% | 3 文件 | api-types.ts + mock_data.json + GCJ02 测试向量 |
| **Web** | ❌ 占位 | 3 文件 | 仅 de/fr/ja i18n JSON，无工程框架 |

---

## 二、✅ 已完成

### Android（app_glm/android）

#### 架构层（全部就绪）
- **Clean Architecture**：data / domain / ui 三层分离
- **7 Repository**：CarRepository、DelegatingCarRepository、RealCarRepository、TeslamateRepository、StatusRepository、SentryStateRepository
- **6 ViewModel**：DashboardViewModel、DriveViewModel、ChargeViewModel、OnboardingViewModel、StatisticsViewModel、TimelineViewModel
- **Hilt DI**：AppModule 完整配置
- **Room**：15 表 + 14 DAO + 完整 Entity
- **Retrofit**：16 端点 + ApiInterceptor
- **RealCarRepository**：Network-First + Room 缓存降级，`DelegatingCarRepository` 代理模式运行时 Mock/Real 切换（已合并 main）

#### 页面（20 Screen 全部实现）
| # | Screen | 文件 | 数据源 |
|---|--------|------|--------|
| 1 | Dashboard | `DashboardScreen.kt` | ✅ ViewModel → CarRepository（真实） |
| 2 | DriveList | `DriveListScreen.kt` | ✅ DriveViewModel |
| 3 | DriveDetail | `DriveDetailScreen.kt` | ✅ DriveViewModel |
| 4 | ChargeList | `ChargeListScreen.kt` | ✅ ChargeViewModel |
| 5 | ChargeDetail | `ChargeDetailScreen.kt` | ✅ ChargeViewModel |
| 6 | Settings | `SettingsScreen.kt` | ✅ SettingsDataStore（含 Mock/Real 切换） |
| 7 | Onboarding | `OnboardingScreen.kt` | ✅ OnboardingViewModel |
| 8 | Statistics | `StatisticsScreen.kt` | ✅ StatisticsViewModel |
| 9 | DayDetail | `DayDetailScreen.kt` | ✅ StatisticsViewModel |
| 10 | MonthDetail | `MonthDetailScreen.kt` | ✅ StatisticsViewModel |
| 11 | Timeline | `TimelineScreen.kt` | ✅ TimelineViewModel |
| 12 | About | `AboutScreen.kt` | ✅ 静态内容 |
| 13 | BatteryHealth | `BatteryHealthScreen.kt` | ❌ 硬编码 `healthPercent=93.5f` |
| 14 | Heatmap | `HeatmapScreen.kt` | ❌ `rememberGridData()` 用 `random()` |
| 15 | Efficiency | `EfficiencyScreen.kt` | ❌ `rememberEffPoints()` 用 `Random.nextFloat()` |
| 16 | Vampire | `VampireScreen.kt` | ❌ `rememberDrainEvents()` 用 `Random.nextFloat()` |
| 17 | Range | `RangeScreen.kt` | ❌ `rememberTripData()` 硬编码 |
| 18 | Destinations | `DestinationsScreen.kt` | ❌ `rememberDestinations()` 硬编码 |
| 19 | Cost | `CostScreen.kt` | ❌ `rememberMonthlyCost()` 硬编码 |
| 20 | Updates | `UpdatesScreen.kt` | ❌ 硬编码 `listOf(SoftwareVersion(...))` |

#### 平台能力
- **WorkManager**：DataSyncWorker + ChargingNotificationWorker + TpmsPressureWorker
- **通知**：ChargingNotificationManager + SentryNotificationManager
- **前台服务**：ChargingMonitorService
- **Widget**：CarWidget（Glance）+ BatteryWidget + WidgetBackgroundCache
- **中国本地化**：GCJ02Converter + AmapComposeView + TOU 分时电价
- **多语言**：en/zh-Hans/de/fr/ja

#### 测试
- 5 单元测试：MappersTest / RealCarRepositoryTest / DelegatingCarRepositoryTest / UrlSecurityTest + TestUtils
- ViewModel/UI 层零测试

#### 安全
- UrlSecurity 运行时公网 HTTP 拦截
- network_security_config 配置
- 17 轮交叉审核 CRITICAL/HIGH 已清零

### iOS（app_glm/ios）

#### 页面（17 Feature 目录）
| # | Feature | 文件 | 数据源 |
|---|---------|------|--------|
| 1 | Dashboard | `DashboardView.swift` | ⚠️ 部分硬编码（7天趋势） |
| 2 | DriveList | `DriveListView.swift` | ✅ |
| 3 | DriveDetail | `DriveDetailView.swift` | ❌ sin/random 假数据 |
| 4 | ChargeList | `ChargeListView.swift` | ✅ |
| 5 | ChargeDetail | `ChargeDetailView.swift` | ❌ sin/random 假数据 |
| 6 | Battery | `BatteryHealthView.swift` | ⚠️ 缺 range loss 维度 |
| 7 | Cost | `CostView.swift` | ✅ |
| 8 | Destinations | `DestinationsView.swift` | ✅ |
| 9 | Efficiency | `EfficiencyView.swift` | ✅ |
| 10 | Heatmap | `HeatmapView.swift` | ✅ |
| 11 | Range | `RangeView.swift` | ✅ |
| 12 | Statistics | `StatisticsView.swift` | ✅ |
| 13 | Timeline | `TimelineView.swift` | ✅ |
| 14 | Updates | `UpdatesView.swift` | ✅ |
| 15 | Vampire | `VampireView.swift` | ✅ |
| 16 | Settings | `SettingsView.swift` | ⚠️ 缺单位/时区 Picker |
| 17 | Onboarding | `OnboardingView.swift` | ✅ |

#### 平台能力
- **Watch App**：4 文件（ContentView + CircularBatteryView + ComplicationController + WatchConnectivityManager）
- **Widget**：MateLinkWidget.swift
- **PhoneWCSessionManager**：手机-Watch 双向通信
- **NotificationManager**：已实现
- **TariffConfigView**：已实现
- **Vehicle3DView**：已实现
- **AmapView**：已实现

#### 测试
- ❌ 测试目录空壳，零测试

### Shared（app_glm/shared）
- `api-types.ts`：API 类型定义
- `mock_data.json`：开发用 mock 数据
- `gcj02_test_vectors.json`：GCJ-02 转换测试向量

---

## 三、🔴 待完成

### Android 高优先级

| # | 问题 | 文件 | 难度 |
|---|------|------|------|
| **G-1** | 8 个分析页硬编码 mock → 接 RealCarRepository | BatteryHealth / Heatmap / Efficiency / Vampire / Range / Destinations / Cost / Updates | **中** |
| **G-2** | CI/CD 完全缺失 | 无 `.github/workflows/` | 小 |
| **G-3** | 上架配置缺失 | 无 play publisher 插件/隐私政策/截图 | 中 |
| **G-4** | Room schema JSON 未生成 | `schemas/` 目录为空 | 小 |

#### G-1 详情（8 个硬编码页）

```
app/src/main/java/com/teslamatelink/
├── ui/battery/BatteryHealthScreen.kt    → healthPercent=93.5f 硬编码，无 ViewModel
├── ui/heatmap/HeatmapScreen.kt          → rememberGridData() 用 random()
├── ui/efficiency/EfficiencyScreen.kt    → rememberEffPoints() 用 Random.nextFloat()
├── ui/vampire/VampireScreen.kt          → rememberDrainEvents() 用 Random.nextFloat()
├── ui/range/RangeScreen.kt              → rememberTripData() 硬编码
├── ui/destinations/DestinationsScreen.kt → rememberDestinations() 硬编码
├── ui/cost/CostScreen.kt               → rememberMonthlyCost() 硬编码
└── ui/updates/UpdatesScreen.kt          → 硬编码 listOf(SoftwareVersion(...))
```

RealCarRepository 已就绪，SettingsScreen 的 Mock/Real 开关已打通。只需创建 8 个 ViewModel 或直接注入 CarRepository 即可切换数据源。

### iOS 高优先级

| # | 问题 | 详情 | 难度 |
|---|------|------|------|
| **G-5** | 5 页面完全缺失 | CurrentCharge / Mileage / Countries / Sentry / Trips（连占位文件都没有） | 大 |
| **G-6** | Settings 缺单位/时区 UI | UnitSystem enum 存在但 SettingsView 无 Picker | 小 |
| **G-7** | 7 天电量趋势硬编码 | BatteryTrendCard 用 `[75,72,68,70,73,76,78]` | 小 |
| **G-8** | BatteryHealth 缺 range loss | 只有 capacity degradation，无续航里程损失计算 | 小 |

### Web 高优先级

| # | 问题 | 难度 |
|---|------|------|
| **G-9** | 仅为翻译占位（de/fr/ja 3 个 JSON），无工程框架 | 大 |

### 工程化短板

| # | 问题 | 平台 |
|---|------|------|
| E-1 | ViewModel/UI 层零测试 | Android |
| E-2 | 测试目录空壳 | iOS |
| E-3 | CI/CD 双端均无 | 双端 |
| E-4 | 真实 TeslaMate 后端未联调 | 双端 |

---

## 四、🟡 中优先级（跨平台一致性）

| # | 问题 |
|---|------|
| X-1 | CarStatus schema 双端分裂（iOS 扁平 33 字段 vs Android 嵌套 9 子对象） |
| X-2 | 字段命名不一致（id vs carId, color vs exteriorColor） |
| X-3 | 空值策略不一致（iOS 偏非空，Android 全可空） |
| X-4 | shared/api-types.ts 未被双端引用（仅 Web 用） |
| X-5 | ISO8601 解析双端实现方式不同 |

---

## 五、🟢 低优先级（代码质量）

| # | 问题 | 平台 |
|---|------|------|
| Q-1 | Mock 数据量不足（drives=6 需 60，charges=4 需 30） | Android |
| Q-2 | API Client 未显式封装 16 端点（仅通用 fetch） | iOS |
| Q-3 | 离线缓存用 JSON 文件而非 Core Data，TTL 1 天非 30 天 | iOS |
| Q-4 | Dashboard 不读缓存、无 Offline banner | iOS |
| Q-5 | Theme 缺"跟随系统"第三态（仅 isDarkMode 二态） | iOS |
| Q-6 | 2D 车辆图为 SF Symbol 简化（非真实 2D 图） | iOS |

---

## 六、🔵 iOS/Watch 审核遗留（需 Mac/Xcode）

| # | 级别 | 问题 | 文件 |
|---|------|------|------|
| I-1 | CRITICAL | @State 数据竞争 — Task.detached 读 @State | `AnnualReportPDFView.swift` |
| I-2 | CRITICAL | WCSession delegate 线程不安全 | `PhoneWCSessionManager.swift` |
| I-3 | HIGH | DashboardView Timer 未取消（5s 轮询泄漏） | `DashboardView.swift` |
| I-4 | HIGH | writeWidgetData 每 5s 重建 ImageRenderer | `DashboardView.swift` |
| I-5 | HIGH | KeychainHelper.save 丢弃 SecItemAdd 返回值 | `AppState.swift` |
| I-6 | HIGH | Vehicle3DView cacheNodes() fallback 未调用 | `Vehicle3DView.swift` |
| I-7 | HIGH | WatchConnectivityManager 缺 @MainActor | `WatchConnectivityManager.swift` |

---

## 七、执行建议

**第一梯队（1 天，立即见效）：**
- G-1 子集：先改 BatteryHealth / Updates（最简单的 2 个）→ 建立 ViewModel 模式
- G-6：iOS Settings 添加单位/时区 Picker
- G-7：iOS Dashboard 7 天趋势从 API 获取

**第二梯队（2-3 天，核心价值）：**
- G-1 全量：8 个分析页全部接 RealCarRepository（最大工作量）
- G-5 子集：补 3 个核心缺失页（CurrentCharge / Trips / Mileage）

**第三梯队（需外部条件）：**
- G-2：GitHub Actions CI/CD 配置
- G-3：Google Play 上架（需开发者账号）
- G-9：Web 工程初始化
- I-1~I-7：iOS/Watch 审核修复（需 Mac + Xcode）
