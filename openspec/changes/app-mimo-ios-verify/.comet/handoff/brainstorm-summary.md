# Brainstorm Summary

- Change: app-mimo-ios-verify
- Date: 2026-07-01

## Confirmed Facts

- Jovi confirmed the change should proceed from open to the next phase.
- Jovi currently has an iOS phone, but no confirmed Mac/Xcode environment.
- Implementation variant is `app_mimo`.
- `docs/git_ref` is reference material only; `docs/PRD` and `docs/PLAN` drive scope.
- The first iOS milestone is environment verification, not broad feature completion.
- `app_mimo/ios` contains SwiftUI sources, resources, Widget source, Info.plist, and a Podfile.
- No `.xcodeproj` or `.xcworkspace` was detected in `app_mimo/ios`.
- Comet design handoff was generated by script at `.comet/handoff/design-context.md`.

## Confirmed Technical Approach

Create or restore a reproducible Xcode entry point for `app_mimo/ios`, include the existing MateLink app target inputs, document simulator and physical-device verification, and record local blocker evidence because the current environment has Windows + iPhone but no Mac/Xcode.

Jovi confirmed the first implementation path:

1. Prepare the iOS project/workspace entry point and verification chain from Windows.
2. Record that native SwiftUI deployment to iPhone is blocked until a Mac/Xcode environment is available.
3. Later, when Jovi gets access to a Mac, run the documented simulator and connected-iPhone verification steps.

## Key Trade-offs And Risks

- Windows local environment cannot run Xcode; verification must distinguish local blocker evidence from Mac/Xcode proof.
- Including Widget in the first target may increase project setup complexity; it may be better to document/defer Widget target if the app target is the immediate blocker.
- CocoaPods AMap dependencies may require macOS tooling; first verification may need either a workspace path or a documented pod-install path.

## Testing Strategy

- Validate OpenSpec artifacts with `openspec validate app-mimo-ios-verify --strict`.
- Use file inventory checks to prove project metadata includes required sources.
- On macOS, run `pod install` if needed and use `xcodebuild` for simulator verification.
- On physical device, verify signing/team/device trust prerequisites before launch.

## Spec Patch

No patch currently proposed. Existing delta spec already covers project entry, simulator build, connected-device verification, and evidence recording.

## Pending Confirmation

- Whether the first Xcode entry point should include Widget target immediately or defer Widget target until the app target launches.
- Whether AMap pods must be active in the first build verification or can be documented while using MapKit fallback first.
